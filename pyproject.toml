[project]
name = "ai-card-project-starter"
version = "0.1.0"
description = "Localhost-only ComfyUI API runner with named generation profiles"
requires-python = ">=3.13,<3.14"
dependencies = [
    "requests>=2.31,<3",
    "huggingface-hub>=1.0,<2",
]

[tool.uv]
package = false